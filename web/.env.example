# CockroachDB connection string (Postgres wire-compatible), pointed at the
# legislative_tracker database within the existing cluster.
# Example: postgresql://user:password@host:26257/legislative_tracker?sslmode=verify-full&options=--cluster%3Dyour-cluster-name
DATABASE_URL=
